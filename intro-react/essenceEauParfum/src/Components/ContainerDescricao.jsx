import { Children } from "react"


const ContainerDescricao = ({children}) => {
    return(
        <div className="container-descricao">
            {children}
        </div>
    )
}

export default ContainerDescricao